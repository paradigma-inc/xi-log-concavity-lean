# Complete rational range checks at the first two grid nodes

Use the certified root table for all 400 bases and the uniform $32$ Gamma-log bound at scale $256$. Kernel computation discharges both pi-log ranges, both dyadic-power ranges and the positive Xi bracket denominator at $k=0,1$. Assemble all fields of the root-based Xi evaluator contract with parameters $(400,440,480,400,256,128)$ and $B=10^{180}$. These checks establish evaluator applicability, not the source-sample accuracy by themselves.
