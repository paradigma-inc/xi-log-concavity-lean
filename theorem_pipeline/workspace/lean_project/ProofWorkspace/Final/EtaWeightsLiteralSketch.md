# Exact Euler weights

The 400 signed rational literals equal the accelerated eta coefficients $(-1)^j w_j$ of `etaSignedWeightTable 400`. Lean's ordinary kernel checks equality of the complete list; runtime generation supplies proposals only. The proof enables reuse of the same weights at every real grid argument and preserves the existing directed remainder $2^{-400}$. No retained Xi sample bound follows from this equality alone.
