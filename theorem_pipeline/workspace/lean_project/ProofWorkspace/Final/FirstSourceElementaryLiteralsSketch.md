# Kernel-checked pi-power and Xi bracket endpoint literals

Rewrite the rounded logarithm of $\pi$ through its already certified literal bounds. The scan/integer exponential implementation is exactly equal to the accepted one. Kernel computation checks both endpoint pairs for $\pi^{-s/2}$ and the positive eta bracket $|s-1|/|1-2^{1-s}|$ at $s=1/2,41/80$, at unchanged precision.
