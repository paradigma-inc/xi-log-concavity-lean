# EtaInitialStates

The 400 literal root states equal the accepted outward-rounded root initialization at $k=0$, with integer grid $B=10^{180}$ and initial exponent $40$. Each row stores the certified lower and upper reciprocal $80$th-root endpoints and its two directed rounded powers. The displayed values are proposals until `decide +kernel` proves the whole list equality. This module certifies that equality; it does not by itself certify later grid samples. Subsequent exact state advancement is justified by `EtaStreamFull`.
