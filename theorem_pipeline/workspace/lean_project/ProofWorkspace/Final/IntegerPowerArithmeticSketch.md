# Integer rounded powers and scanned exponentials

At grid $B>0$, multiplying two stored numerators and taking integer floor or ceiling gives exactly the original outward-rounded rational product. Induction proves the integer power recurrence equals every accepted rounded power iterate. The scanned Taylor sum equals the original sum, so the integer-power exponential has exactly the same endpoints and analytic remainder; no approximation is introduced by this implementation change.
