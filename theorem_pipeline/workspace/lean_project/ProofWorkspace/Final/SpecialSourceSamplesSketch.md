# The removable source node and zero node

The previously certified Gamma, eta, pi-power and bracket intervals at $s=1/2$ give an unconditional enclosure for the actual $\xi(1/2)$. The exact identity $\xi(1)=1/2$ then yields $\varphi(1)=2\xi(1/2)$, so retained node $k=40$ does not require evaluation through a vanishing eta denominator.

The displayed source-rounded midpoint is obtained by the source's 160-digit round-to-nearest operation on JSONL row $k=40$. Lean checks that twice the certified central-Xi interval lies inside that midpoint plus or minus $2\cdot10^{-120}$. Interval inclusion gives the actual discrepancy bound. At $k=0$, the exact identity $\varphi(0)=1$ handles the source midpoint immediately.

These are unconditional actual sample bounds, not bounds conditional on numerical output. They add nodes $k=0,40$ to the already certified $k=1$; they do not certify the other retained nodes or the full density theorem.
