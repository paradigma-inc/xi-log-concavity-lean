# Certified pi-root seed

The two displayed integers $L,U$, on scale $B=10^{180}$, satisfy the exact kernel-checked inequalities $(p+10^{-150})(L/B)^{160}\le1\le(p-10^{-150})(U/B)^{160}$. Both endpoints are nonnegative. The already proved source-pi bracket and root monotonicity therefore enclose the actual $\pi^{-1/160}$.

For every natural $k$, the existing outward-rounded power recurrence at exponent $k+40$ yields an actual enclosure of $\pi^{-(k+40)/160}$. This proof removes repeated pi logarithm/exponential computation from retained-node evaluation. It does not by itself certify any reciprocal-Xi sample. The proposed integers were generated locally, but their polynomial comparisons are checked by Lean's kernel, not trusted runtime evaluation.
