# Shared-root evaluator for actual retained samples

At node $k$, use the certified $\pi^{-1/160}$ seed at exponent $k+40$, the certified integer-base root table for the accelerated eta sum and for $2^{-s}$, and the accepted Gamma recurrence and log-series bounds. Here $s=(k+40)/80$. The Gamma log range and all 400 eta root hypotheses are already discharged uniformly.

The source's directed eta remainder $2^{-400}$ remains in the upper endpoint. The eta bracket is enclosed from the same base-two root powers, requiring a positive rational lower denominator. At $k=40$, use $\xi(1)=1/2$ exactly. Combining these factors with outward rounding encloses the actual $\xi(s)$.

Use the already unconditional certified $\xi(1/2)$ interval as numerator, divide by the positive Xi denominator interval, and round outward to enclose the actual $\varphi(k/40)$. The final theorem turns exact rational containment in $v_k\pm2\cdot10^{-120}$ into the actual sample discrepancy. The two per-node positivity checks and final rational containment remain explicit, not assumed to have passed across the whole retained grid.

The root-based algorithm replaces repeated logarithmic evaluation while preserving the actual functions, source midpoints and error target. It need not numerically equal the old logarithmic endpoint algorithm. Finite endpoint literals still require kernel checking.
