# Kernel-checked eta endpoint literals

For each of $k=0,1$, check the lower and upper integer numerator sums independently. Reconstruct the rational interval using $B=10^{180}$ and the original $2^{-400}$ directed Euler tail. Rewrite through the proved integer implementation equality and the exact Euler-weight table identity. Thus both literal endpoint pairs equal the sound root-based eta evaluator; runtime-generated numbers are not accepted without these kernel proofs.
